// The DOM is the entire white space(screen) in the browser
// DOM is basically document. The doc is just an object
// We need the DOM in order to change, add or delete html in browser.
// window is the parent of document ...window.document();
// window is an obj that describes the browser. window is the biggest parent
// the browser has a window object
// Learn the DOM very well

document.write("helloooo");
// we have a document the accepts an a method "write" that has an argument "helloooo"